services:
  - type: web
    name: charitys-measure
    env: python
    pythonVersion: 3.12.4
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
